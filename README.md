# MsgProcessingJS

## project Messaging
- focuses on pure lamina implementation of messaging
- it has no vscode dependencies so it runs with no issues
- any messaging that uses the vscode infrastructure cannot run headlessly as it requires the vscode runtime environment

## the eventual idea
- when messaging works in pure scalajs...
- create a vscode extension MessageDispatcher implementation of Mwhich delegates all messaging events through vscode extension api

